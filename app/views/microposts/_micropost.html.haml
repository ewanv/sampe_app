%li
	%span.content= add_reply_links_to(wrap(micropost.content))
	%span.timestamp
		Posted
		= time_ago_in_words(micropost.created_at)
		ago.
	= render 'shared/micropost_delete_link', object: micropost
